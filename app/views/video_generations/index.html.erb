<%= render 'layouts/header' %>

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">動画作成</h1>

  <div class="mb-6">
    <p class="text-gray-600 mb-4">ミキシング済みの録音を選択して、動画を作成しましょう。</p>
  </div>

  <% if @recordings.empty? %>
    <div class="alert alert-info">
      <p>ミキシング済みの録音がありません。</p>
      <p class="mt-2"><%= link_to "録音ページへ", recordings_path, class: "btn btn-primary" %></p>
    </div>
  <% else %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% @recordings.each do |recording| %>
        <div class="card bg-base-100 shadow-xl">
          <div class="card-body">
            <h2 class="card-title"><%= recording.title %></h2>
            <p class="text-sm text-gray-600">
              作成日: <%= recording.created_at.strftime("%Y/%m/%d") %>
            </p>
            
            <% if recording.generated_audio.attached? %>
              <audio controls class="w-full mt-2">
                <source src="<%= rails_blob_path(recording.generated_audio) %>" type="audio/mpeg">
              </audio>
            <% end %>
            
            <div class="card-actions justify-end mt-4">
              <%= link_to "この録音で動画作成", new_video_generation_path(recording_id: recording.id), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  
  <div class="mt-8">
    <%= link_to "← ホームに戻る", root_path, class: "btn btn-ghost" %>
  </div>
</div>
