<%= render 'layouts/header' %>

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">動画作成</h1>
  
  <div class="mb-6">
    <p class="text-gray-600 mb-4">
      ミキシング済みの録音を選択して、動画を作成しましょう。
    </p>
  </div>
  
  <% if @recordings.empty? %>
    <!-- 録音がない場合 -->
    <div class="alert alert-info">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <div>
        <p class="font-bold">ミキシング済みの録音がありません</p>
        <p class="text-sm">先に録音をミキシングしてから、動画を作成できます。</p>
      </div>
    </div>
    
    <div class="mt-6">
      <%= link_to "録音ページへ", recordings_path, class: "btn btn-primary" %>
    </div>
    
  <% else %>
    <!-- 録音リスト -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @recordings.each do |recording| %>
        <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow">
          <div class="card-body">
            <h2 class="card-title text-lg">
              <%= recording.title %>
            </h2>
            
            <p class="text-sm text-gray-500">
              作成日: <%= recording.created_at.strftime("%Y年%m月%d日") %>
            </p>
            
            <% if recording.generated_audio.attached? %>
              <div class="mt-3">
                <p class="text-xs text-gray-500 mb-2">プレビュー:</p>
                <audio controls class="w-full" style="height: 40px;">
                  <source src="<%= rails_blob_path(recording.generated_audio) %>" type="audio/mpeg">
                </audio>
              </div>
            <% end %>
            
            <div class="card-actions justify-end mt-4">
              <%= link_to "この録音で動画作成", 
                          new_video_generation_path(recording_id: recording.id), 
                          class: "btn btn-primary btn-sm" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  
  <div class="mt-8">
    <%= link_to "← ホームに戻る", root_path, class: "btn btn-ghost" %>
  </div>
</div>